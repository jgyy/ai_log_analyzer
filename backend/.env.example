AI_PROVIDER=gemini
GEMINI_API_KEY=your-gemini-key-here
VM_CREDENTIAL_ENCRYPTION_KEY={generate via python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"}
ANTHROPIC_API_KEY=your-anthropic-key-here
JWT_SECRET=your-secret
DATABASE_URL=sqlite:///./devops_ai.db

# Comma-separated list of origins allowed to call this API (CORS).
# Local dev default lets the Next.js dev server (localhost:3000) through.
# In production, set this to your deployed frontend's URL, e.g.:
#   CORS_ORIGINS=https://your-app.vercel.app
CORS_ORIGINS=http://localhost:3000
