<!-- ai_log_analyzer frontend/app/, SKILLS.md. -->

# Skills: frontend/app/

| Task | Where |
|---|---|
| Add a new authenticated view | `dashboard/` — keep the "Analyze" trigger in `../components/LogUploader.tsx`, don't duplicate it per page |
| Change the auth screen | `login/` |
