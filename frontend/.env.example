# URL of the deployed backend API. Leave unset for local dev — lib/api.ts
# defaults to http://localhost:8000. Set this in Vercel's project settings
# (Environment Variables) to your production backend's URL, e.g.:
#   NEXT_PUBLIC_API_URL=https://your-backend.example.com
NEXT_PUBLIC_API_URL=http://localhost:8000
